[build]
  functions = "netlify/functions"
  publish   = "."

[functions]
  node_bundler = "esbuild"
  # QW SendEmail can sit 15-30s server-side; give the whole function 90s.
  # Requires a Pro+ plan (background functions or synchronous >26s).
  timeout = 90
