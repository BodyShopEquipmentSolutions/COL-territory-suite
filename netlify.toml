[build]
  functions = "netlify/functions"
  publish   = "."

[functions]
  node_bundler = "esbuild"
  # Function timeout (90s) is configured in the Netlify UI
  # (Project configuration -> Functions -> Timeout) because
  # netlify.toml does not support scalar `timeout` here.
